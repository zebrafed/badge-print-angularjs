'use strict';

describe('myApp.paper-badges module', function() {

  beforeEach(module('myApp.paper-badges'));

  describe('paper-badges controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var paperbadgesCtrl = $controller('paper-badgesCtrl');
      expect(paperbadgesCtrl).toBeDefined();
    }));

  });
});