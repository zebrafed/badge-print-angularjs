'use strict';

describe('myApp.paper-badge module', function() {

  beforeEach(module('myApp.paper-badge'));

  describe('paper-badge controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var paperbadgeCtrl = $controller('paper-badgeCtrl');
      expect(paperbadgeCtrl).toBeDefined();
    }));

  });
});