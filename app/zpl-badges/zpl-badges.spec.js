'use strict';

describe('myApp.zpl-badges module', function() {

  beforeEach(module('myApp.zpl-badges'));

  describe('zpl-badges controller', function () {
    it('should ....', inject(function ($controller) {
      //spec body
      var zplBadgesCtrl(zplBadgesCtrl).toBeDefined();
    }));
  });
});