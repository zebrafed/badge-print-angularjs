'use strict';

describe('myApp.zpl-badge module', function() {

  beforeEach(module('myApp.zpl-badge'));

  describe('zpl-badge controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var zplbadgeCtrl = $controller('zpl-badgeCtrl');
      expect(zplbadgeCtrl).toBeDefined();
    }));

  });
});